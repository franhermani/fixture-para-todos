def main():
    distances_path = "distancias.csv"
    derbies_path = "clasicos.csv"
    distances = {}
    derbies = {}
    read_distances(distances_path, distances)
    read_derbies(derbies_path, derbies)
    print(distances)


def read_distances(path, distances):
    return 0


def read_derbies(path, derbies):
    return 0


main()
